namespace Ineval.DAL
{
    public enum EstadoEnum : byte
    {
        Activo = 1,
        Eliminado,
        Inactivo
    }
    public enum EstadoEnumPeriod : byte
    {
        Abierto = 1,
        Cerrado,
    }
}